"""Casos de prueba de `rag/arbitration.py`.

Pendiente: verificar que, dados dos fragmentos de fechas distintas sobre el
mismo punto normativo, se prioriza el más reciente y el otro se conserva
como histórico — usando fragmentos mezclados de ambas colecciones para
confirmar que el arbitraje ignora el origen de colección.
"""
